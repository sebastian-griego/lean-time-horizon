# lt-106: Formalize a suffix specification

Edit `Task.lean` only. Replace the placeholder body of `SuffixSpec` with a meaningful formalization of:

`suf` is a suffix of `xs` when `xs` can be obtained by prepending some prefix before `suf`.

Then prove the two public lemmas. Do not change declaration names or imports. You may add helper lemmas.

Forbidden constructs include `sorry`, `admit`, `axiom`, `constant`, `opaque`, `unsafe`, custom syntax or macro elaboration, `run_cmd`, `run_tac`, `Lean.addDecl`, `#eval`, and `#exit`.
