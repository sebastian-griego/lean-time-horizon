# lt-115: Direct theorem proving with repeated right cancellation

Edit `Task.lean` only. Prove the cancellation theorem without changing its name or statement.

This task is intentionally a direct theorem-proving row. It is included as one part of the portfolio, not as the dominant task style.

You may add helper lemmas in `Task.lean`. Do not change imports.

Forbidden constructs include `sorry`, `admit`, `axiom`, `constant`, `opaque`, `unsafe`, custom syntax or macro elaboration, `run_cmd`, `run_tac`, `Lean.addDecl`, `#eval`, `#exit`, and `omega`.
