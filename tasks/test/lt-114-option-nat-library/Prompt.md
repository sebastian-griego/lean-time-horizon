# lt-114: Build a tiny Option Nat utility library

Edit `Task.lean` only. Complete the utility functions and proofs for default extraction and successor mapping on `Option Nat`.

Do not change declaration names, theorem statements, or imports. You may add helper lemmas.

Forbidden constructs include `sorry`, `admit`, `axiom`, `constant`, `opaque`, `unsafe`, custom syntax or macro elaboration, `run_cmd`, `run_tac`, `Lean.addDecl`, `#eval`, and `#exit`.
