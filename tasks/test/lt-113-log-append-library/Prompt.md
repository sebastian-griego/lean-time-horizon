# lt-113: Build a tiny append-only log library

Edit `Task.lean` only. Complete the append-only log library by proving append identity, associativity, and length accounting.

Do not change declaration names, theorem statements, imports, or the log representation. You may add helper lemmas.

Forbidden constructs include `sorry`, `admit`, `axiom`, `constant`, `opaque`, `unsafe`, custom syntax or macro elaboration, `run_cmd`, `run_tac`, `Lean.addDecl`, `#eval`, and `#exit`.
