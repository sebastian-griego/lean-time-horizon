# lt-107: Formalize a contains-all list relation

Edit `Task.lean` only. Replace the placeholder body of `ContainsAllSpec` with a meaningful formalization of:

Every element in `needles` also appears in `haystack`.

Then prove the public reflexivity and right-extension lemmas. Do not change declaration names, theorem statements, or imports. You may add helper lemmas.

Forbidden constructs include `sorry`, `admit`, `axiom`, `constant`, `opaque`, `unsafe`, custom syntax or macro elaboration, `run_cmd`, `run_tac`, `Lean.addDecl`, `#eval`, and `#exit`.
