# lt-108: Formalize adjacent nondecreasing order

Edit `Task.lean` only. Replace the placeholder body of `AdjacentLeSpec` with a meaningful recursive formalization of:

Every adjacent pair in the list is nondecreasing.

Then prove the three public constructors. Do not change declaration names, theorem statements, or imports. You may add helper lemmas.

Forbidden constructs include `sorry`, `admit`, `axiom`, `constant`, `opaque`, `unsafe`, custom syntax or macro elaboration, `run_cmd`, `run_tac`, `Lean.addDecl`, `#eval`, and `#exit`.
