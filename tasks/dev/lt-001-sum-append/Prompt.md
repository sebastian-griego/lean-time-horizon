# lt-001: Append correctness for a list sum

Edit `Task.lean` only. Prove the target theorem without changing its name or statement.

You may introduce helper lemmas in `Task.lean`. Do not change imports. The submission is checked with Lean 4.28.0.

Forbidden constructs include `sorry`, `admit`, `axiom`, `constant`, `opaque`, `unsafe`, custom syntax or macro elaboration, `run_cmd`, `run_tac`, `Lean.addDecl`, `#eval`, and `#exit`.

Deliverable: a complete proof that `sumList` distributes over append.
